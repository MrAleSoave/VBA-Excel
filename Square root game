Dim num0 As Variant
Dim num1 As Variant
Dim sqr As Variant

  num0 = InputBox("insert firt number")
  num1 = InputBox("insert second number")
  sqr = Sqr(num)

If num0 = (num1 * num1) Then

  MsgBox ("yes the second number is the sqare root")
  
Else

  MsgBox ("no the sqare root is = " & sqr)
  
End If
